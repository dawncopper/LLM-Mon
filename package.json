{
  "name": "llm-mon-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend:install": "cd backend && npm install",
    "backend:build": "cd backend && npx prisma generate && npm run build",
    "backend:start": "cd backend && npm start",
    "backend:db-push": "cd backend && npx prisma db push",
    "frontend:install": "cd src && npm install",
    "frontend:build": "npm run build",
    "build": "npm run backend:install && npm run backend:build"
  },
  "engines": {
    "node": ">=18"
  }
}